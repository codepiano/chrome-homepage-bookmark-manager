# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

单一主用户：在本机 Chrome 中日常浏览网页的个人用户。用户打开一个新标签页时，希望无需跳转或等待即可找到并打开常用网址，也需要在同一处持续维护自己的网址书签。

## Product Purpose

Local Speed Dial 是一个本地优先的 Chrome 新标签页书签主页。它让用户把常用网址按标签组织、快速访问，并在需要时完成新增、编辑、重排、移动和删除等书签管理操作。

成功意味着：打开新标签页后，用户能快速识别当前标签下的常用链接并直接访问；书签管理操作清楚、可发现且不会误触打开链接。

## Positioning

与只依赖浏览器内置书签或云同步的主页不同，产品以本机 Fastify/SQLite 后端作为书签、展示设置和点击事件的权威数据源；扩展负责新标签页操作体验，数据不写回 Chrome 原生书签。

## Operating Context

- 主要入口是 Chrome 的新标签页。
- 仅面向桌面端 Chrome 扩展环境；不提供移动端插件能力，也不把移动端适配作为发布标准。
- 高频工作流：选择标签、打开常用链接；新增或编辑网址；拖拽整理标签和链接；查看访问次数与最近访问时间。
- 本机服务默认监听 `127.0.0.1:3721`，扩展通过一次性配对令牌访问。
- 服务暂不可用时，扩展展示最近成功同步的只读快照，已展示链接仍可尝试打开。

## Capabilities and Constraints

- 支持标签和链接的新增、编辑、删除、排序及链接跨标签移动。
- 链接可保存 HTTP(S) 地址和 Chrome 内置页地址；仅 HTTP(S) 地址尝试抓取标题、简介和 favicon。
- 不读取、不导入或回写 Chrome 原生书签；不做账户、协作或跨设备同步。
- 不使用网页缩略图；以文字和 favicon 为主。
- 点击链接记录本地访问事件；对链接打开行为不做额外预设。
- API 仅绑定 loopback，除健康检查外均需要本机 bearer token。

## Evidence on Hand

- 已实现的扩展界面：`apps/extension/src/main.tsx` 和 `apps/extension/src/styles.css`。
- 后端、SQLite 数据模型和元数据防护：`apps/server/src/`。
- 当前可加载的 Chrome 扩展产物：`apps/extension/dist/`。
- 无外部品牌资产、商业客户证明或可使用的网页缩略图素材；后续设计不得虚构这些内容。

## Product Principles

1. 新标签页首先服务于立即访问，管理功能不应遮蔽常用链接。
2. 每个操作都应绑定明确资源，并提供可发现的入口与失败反馈。
3. 本机数据、服务连接和自动抓取状态必须如实可见，不能把离线或失败伪装成空状态。
4. 用户自定义内容优先于自动抓取结果，系统自动化只做补齐。
5. 频繁操作保持直接、紧凑且支持键盘访问。

## Accessibility & Inclusion

主要界面操作应具备可见焦点、可访问名称和键盘入口；颜色不是状态的唯一表达方式。
