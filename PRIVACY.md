# Local Speed Dial 隐私说明

Local Speed Dial 是本地优先的软件。项目本身不提供账号、广告、遥测、云数据库或跨设备同步服务。

本说明描述当前源码版本的默认行为。自行修改代码、配置代理、开放本机端口或接入第三方自动化工具，可能改变数据流向。

## 保存的数据

本机服务会在仓库的 `data/` 目录中保存：

- 文件夹、排序和自动归集规则；
- 书签 URL、标题、描述、图标和外观设置；
- 页面显示设置；
- 点击统计；
- 从 Chrome 同步的浏览历史归档；
- 网页信息抓取缓存；
- 本机 API 配对 Token。

Chrome 扩展也会使用 `chrome.storage.local` 保存连接设置、只读离线快照，以及服务离线期间等待重试的历史事件。

## 网络访问

本机 API 默认只监听 `127.0.0.1:3721`。除健康检查外，请求需要 Bearer Token。

添加或更新书签时，本机服务可能访问目标网页来获取标题、描述和图标。这类请求直接从你的电脑发出；目标网站、DNS 服务和你主动配置的网络代理可能看到请求。抓取失败不会阻止保存或打开书签。

项目没有运营方控制的远程数据接收服务。点击书签打开第三方网站时，后续数据处理由该网站自己的隐私政策决定。

## Chrome 权限

| 权限 | 用途 |
| --- | --- |
| `storage` | 保存配对信息、离线快照和待重试队列 |
| `history` | 首次导入并持续同步 Chrome 浏览历史到本机数据库 |
| `alarms` | 定期重试尚未同步的历史事件 |
| 本机 HTTP 访问 | 连接 `127.0.0.1` / `localhost` 上的本机 API |

当前扩展清单还声明了 `tabs` 和广泛的 HTTP(S) 主机访问权限，但现有主要打开行为使用普通链接，网页信息抓取也由本机服务完成。正式提交扩展商店前，应再次审计并移除无法由实际功能证明必要性的权限；隐私说明不能代替最小权限原则。

历史记录不会由本项目上传到云端。用户在 Chrome 中删除历史后，本机归档会保留记录并标记其 Chrome 来源已删除，以维持本机资料库的连续性。

## 导出

便携 JSON 导出可以包含文件夹、规则、链接、卡片外观和设置，但不会包含：

- API Token；
- 浏览历史；
- 点击事件；
- 网页信息缓存；
- 本机绝对路径。

导出文件由用户自行保管。它可能包含私人网址、标题或笔记，不应随意上传到公开位置。

## 删除数据

1. 运行 `./scripts/stop.sh` 停止本机服务；
2. 在 `chrome://extensions/` 中移除扩展，这会清除该扩展的浏览器本地存储；
3. 确认不再需要备份后，手动删除仓库中的 `data/` 目录。

删除 `data/` 会永久移除数据库和 Token。建议先备份，并确认没有正在运行的服务仍在写入。

## 安全建议

- 不要把 `data/api-token` 提交到版本控制或发给他人；
- 不要把本机 API 端口映射到公网；
- 只从可信来源安装或构建扩展；
- 给第三方脚本或 AI 工具使用 API 前，先了解它会读取或写入什么；
- 在批量导入或更新前导出备份。

如发现隐私或安全问题，请通过仓库的 Issue 渠道提供最小复现信息；不要在公开 Issue 中附上 Token、私人 URL、完整数据库或未经清理的日志。
